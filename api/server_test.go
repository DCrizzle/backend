package api

import "testing"

func Test_serverStartStop(t *testing.T) {
	// s := newServer("127.0.0.1:8080")
	//
	// if err := s.start(); err != nil {
	// 	t.Errorf("description: error starting server, received: %v", err)
	// }
	//
	// if err := s.stop(); err != nil {
	// 	t.Errorf("description: error stopping server, received: %v", err)
	// }
}
